# FluXTape — participatory version system
# Python 3.9+

# Core interface
streamlit>=1.32.0

# Audio processing
pydub>=0.25.1

# Cloud storage (set LOG_BACKEND=s3 or LOG_BACKEND=both to activate)
boto3>=1.34.0

# Data handling / analysis
pandas>=2.0.0
numpy>=1.26.0

# Audio feature extraction (for analysis scripts in data/)
librosa>=0.10.0

# Source separation (optional — used by audio/separate.py)
# Requires torch; install separately if needed:
# pip install torch torchaudio
# demucs>=4.0.0

# Testing
pytest>=8.0.0
